server:
  port: 3377

spring:
  application:
    name: nacos-config-client
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yaml # 指定yaml 格式的配置
        namespace: 47cd6cec-c866-4478-b0b4-ccb16688ec25

# nacos-config-client-dev.yaml

management:
  endpoints:
    web:
      exposure:
        include: "*"